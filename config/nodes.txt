# Node IP Mapping
# Format: hostname=private_ip
#
# Edit this file with your actual node private IPs
# This will be used to generate /etc/hosts entries

minio1=10.0.0.3
minio2=10.0.0.5
minio3=10.0.0.4
minio4=10.0.0.2

# Expansion nodes (add when expanding)
# minio5=10.0.0.5
# minio6=10.0.0.6
# minio7=10.0.0.7
# minio8=10.0.0.8

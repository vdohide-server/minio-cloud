# MinIO Cluster Configuration
# ============================
# ไฟล์หลักสำหรับจัดการ MinIO Cluster
#
# วิธีใช้:
#   1. แก้ไขไฟล์นี้ (เพิ่ม pool, แก้ IP)
#   2. รัน: ./update-nodes.sh --dry-run    (ดูก่อน)
#   3. รัน: ./update-nodes.sh --restart    (อัพเดท + restart)

# ============================
# Credentials (ต้องเหมือนกันทุก node)
# ============================
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=!zX21042537
MINIO_SITE_REGION=cloud
MINIO_SITE_NAME=minio-cluster

# ============================
# Pool 1: Nodes 1-4
# ============================
POOL1_START=1
POOL1_END=4
POOL1_DISKS=1
POOL1_PATH=/mnt/minio-data

NODE1_IP=10.0.0.3
NODE2_IP=10.0.0.5
NODE3_IP=10.0.0.4
NODE4_IP=10.0.0.2

# ============================
# Pool 2: Nodes 5-8 (uncomment เมื่อเพิ่ม)
# ============================
# POOL2_START=5
# POOL2_END=8
# POOL2_DISKS=1
# POOL2_PATH=/mnt/minio-data

# NODE5_IP=10.0.0.8
# NODE6_IP=10.0.0.7
# NODE7_IP=10.0.0.9
# NODE8_IP=10.0.0.6

# ============================
# Pool 3: Nodes 9-12 (uncomment เมื่อเพิ่ม)
# ============================
# POOL3_START=9
# POOL3_END=12
# POOL3_DISKS=4
# POOL3_PATH=/data/disk
#
# NODE9_IP=10.0.0.10
# NODE10_IP=10.0.0.11
# NODE11_IP=10.0.0.12
# NODE12_IP=10.0.0.13
